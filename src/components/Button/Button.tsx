// "use client";

// import { toast } from "react-toastify";

// const Button = ({ children }) => {
//   return <button onClick={() => toast.success("успешно")}>{children}</button>;
// };

// export default Button;
